[project]
name = "just-focus"
version = "1.1.0"
description = "Vector electromagnetic field calculations in the focus of high NA microscope objectives."
readme = "README.md"
license = "GPL-3.0-or-later"
license-files = ["LICENSE"]
authors = [
    { name = "Kyle Douglass", email = "kyle.douglass@epfl.ch" }
]
keywords = ["microscopy", "electromagnetism", "richards-wolf", "diffraction"]
classifiers = [
  # How mature is this project? Common values are
  #   3 - Alpha
  #   4 - Beta
  #   5 - Production/Stable
  "Development Status :: 4 - Beta",

  "Intended Audience :: Science/Research",
  "Topic :: Scientific/Engineering :: Physics",

  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
]
requires-python = ">=3.11"

dependencies = [
    "numpy>=2.3.2",
]

[project.urls]
Repository = "https://github.com/LEB-EPFL/just-focus"

[project.optional-dependencies]
plot = [
    "matplotlib>=3.10.5",
    "pyside6>=6.9.1",
]
zernike = [
    "zernipax>=0.2.1",
]
torch = [
    "torch>=2.2",
]

[project.scripts]
gaussian = "leb.just_focus.scripts.gaussian:main"
halfmoon = "leb.just_focus.scripts.halfmoon:main"
aberrated_halfmoon = "leb.just_focus.scripts.aberrated_halfmoon:main"
displaced_gaussian = "leb.just_focus.scripts.displaced_gaussian:main"
focus_stack = "leb.just_focus.scripts.focus_stack:main"

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "ruff>=0.14.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/leb"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
